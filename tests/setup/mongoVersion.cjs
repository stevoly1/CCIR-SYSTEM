// The MongoDB server version used by every in-memory test database (integration tests,
// browser journeys, and the backup rehearsal). Keep it at the latest production release.
module.exports = { MONGODB_TEST_VERSION: '8.3.11' };
